String token = '';
String uId = '';
Map<String, dynamic> data = {};
int i;

// for SHA1 using firebase
// ./gradlew signingReport

// for git updates

// 1. checkout to master branch (ie go back to master if im not in master)
// 2. update master (use the blue arrow up right to merge the files with the ones on git (update project (pull)))
// 3. create branch with the name of the task from down-right with any name
// 4. do work
// 5. commit (the second button on the upper right), then write some message, and commit only
// 6. checkout to master again
// 7. update master
// 8. checkout to local branch
// 9. merge master with local branch from down-right, click on master branch, then choose merge
// 10. push
// 11. go to git and merge stuff
